package com.trio.backend.enums;

/**
 * Document status enum matching {@code Document.DocumentStatus}.
 * Kept as a top-level enum for broader import convenience.
 */
public enum DocumentStatus {
    ACTIVE,
    ARCHIVED,
    DELETED
}
